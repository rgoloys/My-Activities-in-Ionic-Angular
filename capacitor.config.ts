import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'folder_structure',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
